#pragma once

enum class MoveIndicatorMode {
    OnTrigger,
    Rect,
    Cluster,
    Disable
};

enum class DurationLineType {
    Generic,
    Alpha,
    Pulse,
    Color,
    Time,
    Sound,
    Shake,
    None
};

enum class ArrowDirection {
    Up,
    Down,
    Left,
    Right
};